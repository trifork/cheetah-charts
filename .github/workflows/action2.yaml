on: [push]

env:
  DEVOPS_DIR: cheetah-charts-test

jobs:
  print-from-script:
    runs-on: ubuntu-latest

    steps:
      - name: Check-out devops repository
        uses: actions/checkout@v2
        with:
          repository: kasttrifork/cheetah-charts-test
          token: ${{ secrets.GITHUB_TOKEN }}
          path: ${{ env.DEVOPS_DIR }}

      - name: Check latest
        run: |
          out=$(python ${{ env.DEVOPS_DIR }}/.github/scripts/test.py --name "first time")
          echo '::set-output name=SCRIPT::$out'

  job2:
    runs-on: ubuntu-latest
    needs: print-from-script
    steps:
      - run: echo "Venter på print-from-script"

  jobTet:
    runs-on: ubuntu-latest
    steps:
      - name: set-selected-color
        run: echo '::set-output name=SELECTED_COLOR::green'
        id: random-color-generator
      - name: Get color
        run: echo "pleas work ${{ steps.random-color-generator.outputs.SCRIPT }}"
