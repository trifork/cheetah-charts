{{- if .Values.flink.savepoints.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "cheetah-flink-native.fullname" . }}-s3
type: Opaque
data:
  accessKey: {{ .values.flink.savepoints.accessKey | b64enc }}
  secretKey: {{ .values.flink.savepoints.secretKey | b64enc }}
{{- end }}