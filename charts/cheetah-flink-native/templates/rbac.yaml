{{- if .Values.rbac.create }}
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  labels:
    {{- include "cheetah-flink-native.labels" . | nindent 4 }}
  name: {{ include "cheetah-flink-native.fullname" . }}-role
rules:
  - apiGroups:
    - ""
    resources:
    - pods
    - configmaps
    verbs:
    - '*'
  - apiGroups:
    - apps
    resources:
    - deployments
    verbs:
    - '*'
{{- with .Values.rbac.additionalRules }}
  {{- toYaml . | nindent 2 }}
{{- end }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  labels:
    {{- include "cheetah-flink-native.labels" . | nindent 4 }}
  name: {{ include "cheetah-flink-native.fullname" . }}-role-binding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "cheetah-flink-native.fullname" . }}-role
subjects:
- kind: ServiceAccount
  name: {{ include "cheetah-flink-native.serviceAccountName" . }}
{{- end }}
